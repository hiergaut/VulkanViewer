language: cpp

matrix:
    include:

        - name: "Linux gcc"
          os: linux
          dist: xenial
          compiler: gcc
          before_script:
            - sudo apt-get install -y libxrandr-dev libxinerama-dev libx11-dev libxcursor-dev libxi-dev libgl1-mesa-dev libvulkan1 libvulkan-dev

        # - name: "Linux clang"
        #   os: linux
        #   dist: xenial
        #   compiler: clang

        # - name: "macOS Xcode 10 (Makefiles)"
        #   os: osx
        #   osx_image: xcode10
        #   env:
        #
        - name: "Visual studio 15 2017"
          os: windows
          #arch: x64
          env:


script:
    - mkdir build && cd build
    - cmake ..
    - cmake --build .
